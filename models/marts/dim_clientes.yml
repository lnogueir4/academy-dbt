version: 2
models:
  - name: dim_clientes
    description: Dimensão de clientes da AW.
    columns:
      - name: id_cliente
        description: chave pri cliente na tabela de vendas
        tests:
          - not_null
          - unique
      - name: nome_cliente
      - name: id_territorio        
      - name: nome_loja
      - name: tipo_cliente
        description: se revenda ou consumidor